{
  "name": "oposita-sanidad-app",
  "version": "1.0.0",
  "private": true,
  "description": "Oposita Sanidad · Tests — app nativa (Capacitor) para Google Play",
  "scripts": {
    "sync": "cap sync",
    "open:android": "cap open android",
    "add:android": "cap add android"
  },
  "dependencies": {
    "@capacitor/core": "latest"
  },
  "devDependencies": {
    "@capacitor/cli": "latest",
    "@capacitor/android": "latest"
  }
}
